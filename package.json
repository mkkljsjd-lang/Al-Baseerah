
{
  "name": "al-baseerah-grammar-analyzer",
  "version": "1.0.0",
  "private": true,
  "description": "Advanced Islamic and Arabic Grammar Analyzer",
  "scripts": {
    "start": "serve .",
    "build": "mkdir -p public && cp -rf index.html index.tsx metadata.json types.ts services components public/"
  }
}
